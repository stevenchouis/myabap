*&---------------------------------------------------------------------*
*& Report  ZR_TR01_SYNTAX_BASICS
*& 練習 1：ABAP 語法基礎（答案程式）
*&---------------------------------------------------------------------*
* 這是「整行註解」：行首第一個字元是星號 *，整行都是註解
REPORT zr_tr01_syntax_basics.

* 規則 1：每個 ABAP statement 都以句點「.」結束
WRITE 'Hello ABAP!'.

" 規則 2：這是「行內註解」，從雙引號 " 開始到行尾都是註解
WRITE / '我是第二行'.             " WRITE 後面的 / 表示先換行再輸出

* 規則 3：冒號「:」鏈式寫法
* 多個相同關鍵字的 statement 可以合併成一句，用逗號分隔、最後以句點結束
WRITE: / '鏈式寫法第一行',
       / '鏈式寫法第二行',
       / '鏈式寫法第三行'.

* 規則 4：ABAP 關鍵字不分大小寫，以下三行效果完全相同
write / 'lowercase 寫法'.
Write / 'Mixed Case 寫法'.
WRITE / 'UPPERCASE 寫法'.

* 規則 5：一個 statement 可以跨越多行書寫，
* 編譯器只認句點在哪裡，不在乎換行與縮排
WRITE
  /
  '跨多行的 statement，效果跟寫在一行一樣'.
